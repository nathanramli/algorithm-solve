#include <bits/stdc++.h>

using namespace std;

int main(){
    int t, k;
    long long n;
    cin >> t;
    while (t--)
    {
        cin >> n >> k;
        if(k > n) {
            cout << "NO\n";
            continue;
        }
    }    
}