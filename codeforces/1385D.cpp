#include <bits/stdc++.h>
#define ll long long
using namespace std;
int main(){
    int t;
    cin >> t;
    while (t--)
    {
        int n, len, a_kiri = 0, a_kanan = 0;
        cin >> n;
        string s;
        cin >> s;
        len = s.size();

        if(len == 1) {
            if(s == "a") cout << 0 << '\n';
            else cout << 1 << '\n';
            continue;
        }

        for (int i = 0; i < len; i++)
        {
            
        }
    }
}