#include <bits/stdc++.h>
using namespace std;
int a[3000];
int main(){
    map<pair<int, int>, int> x;
    int t,n;
    cin >> t;
    while (t--)
    {
        cin >> n;
        for(int i=0;i<n;i++)
            cin >> a[i];
        for(int i=0;i<n-3;i++)
    }
    
}